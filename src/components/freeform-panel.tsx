export function FreeformPanel() {
  return <section className="panel theme-panel freeform-panel">
    <div className="theme-panel-content">
      <span className="section-kicker">Current format</span>
      <h2>Freeform</h2>
      <p>No shared theme is active. Members can follow their own prompt for each drop.</p>
    </div>
  </section>;
}
